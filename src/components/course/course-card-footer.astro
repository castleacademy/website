---
import { TagIcon } from 'lucide-react';

export interface Props {
  price: number | string;
  currency?: string;
  originalPrice?: number | string;
  class?: string;
}

const { 
  price, 
  currency = "Rp",
  originalPrice,
  class: className = "" 
} = Astro.props;

const formatPrice = (price: number | string) => {
  if (typeof price === 'number') {
    return price.toLocaleString('id-ID');
  }
  return price;
};
---

<div class={`text-base lg:text-lg flex items-center gap-2 font-semibold mb-2 mt-3 ${className}`}>
  <TagIcon className="size-5 lg:size-6" />
  <span>
    {currency}{formatPrice(price)}
  </span>
  {originalPrice && (
    <span class="line-throughmr-2">
      {currency}{formatPrice(originalPrice)}
    </span>
  )}
</div>
